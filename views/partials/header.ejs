<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="icon" href="/images/favicon.ico">
  <link rel="preconnect" href="https://fonts.gstatic.com">
  <link href="https://fonts.googleapis.com/css2?family=Comfortaa:wght@300&display=swap" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Encode+Sans+Expanded&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/stylesheets/style.css">
  <script defer src="/javascripts/navbar.js"></script>
  <title>Project Playlist</title>
</head>

<body>
  <header>
    <a href="/"><img id="icon" src="/images/project-playlist.svg"></a>
    <nav>
      <div id="nav-middle" class="desktop-links">
        <a class="nav-link nav-link-middle desktop-links" href="/playlists">All Playlists</a>
        <% if (user) { %>
          <a class="nav-link nav-link-middle desktop-links" href="/users/<%= user._id %>">My Playlists</a>
          <a class="nav-link nav-link-middle desktop-links" href="/playlists/new">New Playlist</a>
        <% } %> 
        <a class="nav-link nav-link-middle desktop-links" href="/users">Curators</a>
      </div>
      <% if (user) { %>
        <a class="nav-link desktop-links" href="/logout">Log Out</a>
      <% } else { %>
        <a class="nav-link login desktop-links" href="/auth/google">Log In&nbsp;<img style="height: 20px;" src="https://i.imgur.com/FHjYyi0.png"></a>
      <% } %>
    </nav>
    <img id="hamburger-icon" src="/images/hamburger-icon-open.svg.png" alt="Hamburger menu icon."/>
    <div id="hamburger-links-div">
      <a class="hamburger-link" href="/playlists">All Playlists</a>
      <% if (user) { %>
        <a class="hamburger-link" href="/users/<%= user._id %>">My Playlists</a>
        <a class="hamburger-link" href="/playlists/new">New Playlist</a>
      <% } %> 
      <a class="hamburger-link" href="/users">Curators</a>
      <% if (user) { %>
        <a class="hamburger-link" href="/logout">Log Out</a>
      <% } else { %>
        <a class="login hamburger-link" href="/auth/google">Log In&nbsp;<img style="height: 20px;" src="https://i.imgur.com/FHjYyi0.png"></a>
      <% } %>
    </div>
  </header>